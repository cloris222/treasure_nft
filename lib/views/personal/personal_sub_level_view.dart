import 'package:flutter/material.dart';

class PersonalSubLevelView extends StatelessWidget {
  const PersonalSubLevelView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
