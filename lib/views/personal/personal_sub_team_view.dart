import 'package:flutter/material.dart';

class PersonalSubTeamView extends StatelessWidget {
  const PersonalSubTeamView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
