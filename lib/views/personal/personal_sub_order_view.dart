import 'package:flutter/material.dart';

class PersonalSubOrderView extends StatelessWidget {
  const PersonalSubOrderView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
